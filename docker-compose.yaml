version: '3.8'
services:
  postgres:
    image: postgres:14.2-alpine
    container_name: servo-postgres
    restart: always
    ports:
      - 5432:5432
    env_file:
      - .env
    volumes:
      - postgres:/var/lib/postgresql/data

volumes:
  postgres:
    name: servo-db